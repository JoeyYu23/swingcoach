# Core
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6
httpx>=0.24.0
Pillow>=10.0.0

# Frame extraction requires ffmpeg on PATH (install with: winget install Gyan.FFmpeg)

# Pose estimation (optional — gracefully skipped if unavailable on ARM64 Windows)
mediapipe>=0.10.0
numpy>=1.24.0,<2.0.0
opencv-python>=4.8.0

# Utilities
pydantic>=2.0.0
python-dotenv>=1.0.0

# Gemini AI (optional — only needed when VISION_PROVIDER=gemini)
google-genai>=1.0.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
